# Example Vite environment variables
# Set VITE_API_URL to the full URL of your backend API (no trailing slash), e.g.
# VITE_API_URL=http://localhost:8000
# If empty or omitted, the frontend will use relative paths (same origin).
VITE_API_URL=
